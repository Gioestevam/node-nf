
function createXml(object, action) {
    switch (action) {
        case 'postInvoice':
            
            break;
    
        default:
            break;
    }
}

module.exports = {
    createXml
}